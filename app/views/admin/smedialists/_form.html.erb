<%= form_for(@admin_smedialist) do |f| %>
  <% if @admin_smedialist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_smedialist.errors.count, "error") %> prohibited this admin_smedialist from being saved:</h2>

      <ul>
      <% @admin_smedialist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :admin_service_id %><br>
    <%= f.text_field :admin_service_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
